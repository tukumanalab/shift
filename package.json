{
  "name": "shift-app",
  "version": "1.0.0",
  "description": "Google Login Shift Management App",
  "main": "index.html",
  "scripts": {
    "dev": "npx http-server -p 3000",
    "build": "echo 'No build needed for static files'",
    "preview": "npx http-server -p 3000"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}