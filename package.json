{
  "name": "shift-app",
  "version": "1.0.0",
  "description": "アルバイトシフト管理アプリケーション",
  "main": "index.html",
  "scripts": {
    "dev": "npx http-server -p 8081",
    "build": "echo 'No build needed for static files'",
    "preview": "npx http-server -p 8081",
    "deploy:gas": "cd gas && ./deploy.sh"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}